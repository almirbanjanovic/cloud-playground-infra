# ------------------------------------------------------------------------------
# Placeholder for `import { }` blocks — kept as a dedicated file so any
# ad-hoc `terraform import` operations end up in one obvious location
# (out of `main.tf`). Empty by default.
#
# When you use it: add `import` blocks referencing existing Azure resources
# (usually needed when adopting a resource that was created out-of-band).
# Delete the block from here once `terraform state list` shows the target
# resource address is populated.
# ------------------------------------------------------------------------------
